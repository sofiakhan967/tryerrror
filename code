function multiply(a,b){
    if (isNaN(a)){
        throw{
            error:"a is not a number"
        }
    }
    
}
    try {
        multiply(2,2);
        multiply("ibad",28);
        multiply(6,7);
        
    }
    catch(e){
        console.log(e.error);
        console.log(e.message);
    }
